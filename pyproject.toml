[project]
name = "ai-chat-2-ai"
version = "1.0.0"
description = "AI Multi-Model Discussion System"
authors = [
    { name = "quito96", email = "your.email@example.com" }
]
readme = "README.md"
license = { file = "License" }
requires-python = ">=3.11"
keywords = ["ai", "chat", "discussion", "crewai", "llm"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "crewai>=0.51.1",
    "python-dotenv>=1.0.1",
    "reportlab>=4.2.2",
    "requests>=2.31.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[project.urls]
Homepage = "https://github.com/quito96/Al_Chat_2_Al"
Repository = "https://github.com/quito96/Al_Chat_2_Al.git"
Issues = "https://github.com/quito96/Al_Chat_2_Al/issues"

[project.scripts]
ai-chat = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
exclude = [
    "__pycache__",
    "*.pyc",
    ".env",
    ".venv",
    "venv",
    ".git",
    ".idea",
    "*.log"
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "ruff>=0.1.0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.black]
line-length = 100
target-version = ['py311']
